apiVersion: v1
kind: Service
metadata:
  name: letterboxd-api
  labels:
    app: letterboxd-api
spec:
  type: ClusterIP
  selector:
    app: letterboxd-api
  ports:
    - port: 80
      targetPort: 8081
